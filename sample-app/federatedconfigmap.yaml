apiVersion: types.kubefed.io/v1beta1
kind: FederatedConfigMap
metadata:
  name: web-file
  namespace: demo-app
spec:
  template:
    data:
      index.html: "Hello from KubeFed! This app is federated across clusters."
  placement:
    clusterSelector: {}
