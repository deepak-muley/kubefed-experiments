apiVersion: types.kubefed.io/v1beta1
kind: FederatedNamespace
metadata:
  name: demo-app
  namespace: demo-app
spec:
  placement:
    # Propagate to all member clusters (use clusterSelector: {} for "all")
    clusterSelector: {}
